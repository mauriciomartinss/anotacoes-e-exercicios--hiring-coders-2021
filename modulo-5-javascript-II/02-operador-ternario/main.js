// if...else

if(idade >= 18) {
    return 'maior de idade';
} else {
    return 'menor de idade;'
}

idade >= 18 ? "maior de idade" : "menor de idade"; //se a condicao for verdadeira, a primeira opcao apos o ponto de interrogacao sera a resposta