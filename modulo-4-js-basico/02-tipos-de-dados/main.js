var myName = "mauricio"; //string

var age = 20; //number

var height = 1.80; //number

null
undefined

var isStudent = true; //boolean

// objeto, array, function;

var person = {
    "name": "mauricio",
    "age": "20"
}; //objeto/struct (armazenas valores heterogêneos)

var students = ["maurcio", "martins", "damasceno"]; //array (acho que o mais certo seria vetor, mas depende) (armazena valores homogeneos);

function sum(a,b) {
    return a + b;
} //funcao


// operador typeof

console.log(typeof 32);
console.log(typeof person);


function sum() {
    return 5 + 2;
}

console.log(typeof sum);